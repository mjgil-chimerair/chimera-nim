[package]
name = "rnim_nimble"
edition = "2021"
version = "0.1.0"
description = "Nimble/package integration, dependency graph, `.nimble` parsing, build hooks subset"
license = "0BSD"
repository = "https://github.com/chimera-nim/chimera-nim"

[dev-dependencies]
rnim_allocator = { path = "../rnim_allocator" }

[dependencies]
rnim_span = { path = "../rnim_span" }
serde = { workspace = true }
toml = { workspace = true }
semver = "1.0"
sha2 = "0.10"
