[package]
name = "rnim_mir"
edition = "2021"
version = "0.1.0"
description = "Mid-level IR for control flow, calls, moves, destructors, exceptions, and backend-neutral lowering"
license = "0BSD"
repository = "https://github.com/chimera-nim/chimera-nim"

[dev-dependencies]
rnim_allocator = { path = "../rnim_allocator" }

[dependencies]
petgraph = { workspace = true }
smallvec = { workspace = true }
rnim_span = { path = "../rnim_span" }
rnim_hir = { path = "../rnim_hir" }
