[package]
name = "rnim_stdlib"
edition = "2021"
version = "0.1.0"
description = "Minimal compatible `system`, core modules, and stdlib compatibility shims"
license = "0BSD"
repository = "https://github.com/chimera-nim/chimera-nim"

[dev-dependencies]
rnim_allocator = { path = "../rnim_allocator" }

[dependencies]
rnim_span = { path = "../rnim_span" }
